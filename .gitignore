# Habit data (never commit personal tracking data)
# If an example config is committed later, whitelist it, e.g.:
# !example.json
*.json
*.backup

# macOS
.DS_Store
._*
.AppleDouble
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp
*~

# Claude Code local settings and private notes
.claude/settings.local.json
CLAUDE.local.md
